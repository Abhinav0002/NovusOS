[package]
name = "kernel"
version = "1.0.0"
edition = "2021"
description = "Bare-metal AArch64 monolithic kernel with MMU, scheduler, VirtIO drivers, filesystem, and TCP/IP stack"
repository = "https://github.com/Abhinav0002/NovusOS"
license = "MIT"
